{{#if yes}}A fact, value, listing, or outcome in `regions[{{i}}].text` is needed for the remaining steps of `goal.original_request` or `goal.latest_request` and is not restated in `goal.latest_reply`; or a sentence in `goal.standing_requirements` asks for it to be kept, counted, listed, watched for, or reported, whether or not the reply mentions it.{{else}}The text of `regions[{{i}}]` is consumed, superseded, or unrelated to `goal.original_request` and `goal.latest_request`, no sentence in `goal.standing_requirements` asks for it, and a recovery link suffices.{{/if}}
