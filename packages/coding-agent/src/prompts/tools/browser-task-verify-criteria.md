{{#if yes}}The observation shows the outcome the goal asks for: the result or confirmation page in `page`, or, for a goal that only fills or chooses fields, every such field `filled` in `elements`.{{else}}At least one requirement of the goal has no visible evidence: the expected page or result absent from `page`, a field the goal asked to fill still empty, or a form left unsubmitted.{{/if}}