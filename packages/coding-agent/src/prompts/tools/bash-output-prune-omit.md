Goal: Decide whether `segments[{{i}}]` from `{{command}}` can be omitted without losing any fact that `task.original_request`, `task.latest_request`, `task.latest_reply`, any entry in `task.standing_requirements`, or the person who ran the command may need to act on the result.

Success means assigning high probability only when the complete lines {{line_range}} add routine progress, repetition, or low-value detail already represented by retained output and are unnecessary for every task field. Assign low probability when the segment carries a result, diagnostic, evidence, unique fact, ordering dependency, requested entry, countable item, or progress value relevant to the task or command's evident purpose.

Stop after judging this complete segment against every task field, `output_head`, `output_tail`, and the other complete `segments`.
