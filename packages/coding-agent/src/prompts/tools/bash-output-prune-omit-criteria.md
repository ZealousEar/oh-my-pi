{{#if yes}}Omitting the segment preserves every fact and line needed by `task.original_request`, `task.latest_request`, `task.latest_reply`, every entry in `task.standing_requirements`, and the person acting on the command result; an omission marker is sufficient.{{else}}Retaining the segment preserves a unique result, diagnostic, fact, evidence item, requested or countable entry, progress value, or context needed by a task field or to understand what the command reports.{{/if}}
